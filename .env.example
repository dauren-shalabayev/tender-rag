DATABASE_URL=postgresql://rag:rag@localhost:5437/rag
EMBEDDING_MODEL=intfloat/multilingual-e5-small
# для ingest_local внутри контейнера:
# FILES_DIR=/files

# Профиль по умолчанию для /v1/match и /v1/match/analyze (если не передали profile в JSON)
COMPANY_PROFILE=IT-провайдер в Казахстане: хостинг и colocation в ЦОД, виртуальные серверы (VPS/VDC), облачные вычислительные мощности, резервное копирование и мониторинг инфраструктуры. Работаем с госсектором и бизнесом; география — Республика Казахстан.

# Разбор через OpenAI — ключ только локально, не коммить:
OPENAI_API_KEY=***
OPENAI_CHAT_MODEL=gpt-4o-mini
CORS_ORIGINS=https://tenderflow-admin.vercel.app,http://127.0.0.1:5173,http://localhost:8080